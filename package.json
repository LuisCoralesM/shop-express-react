{
  "scripts": {
    "start": "npm run dev",
    "api": "cd server && npm install --package-lock-only --no-audit --no-fund --no-optional && npm run mig && npm start",
    "dev": "npm run api",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}
